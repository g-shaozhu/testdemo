{
  "extends": ["react-app", "eslint-config-react-app"],
  "rules": {
    "no-multi-spaces": "error"
  },
  "overrides": [
    {
      "files": ["**/*.ts?(x)"],
      "rules": {
        
      }
    }
  ]
}